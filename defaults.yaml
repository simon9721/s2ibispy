# defaults.yaml — global defaults (used automatically by schema.py)
global_defaults:
  temp_range: { typ: 27, min: 0, max: 100 }
  voltage_range: { typ: 1.1, min: 1.0, max: 1.2 }
  pullup_ref: { typ: 1.1, min: 1.0, max: 1.2 }
  pulldown_ref: { typ: 0, min: 0, max: 0 }
  power_clamp_ref: { typ: 1.1, min: 1.0, max: 1.2 }
  gnd_clamp_ref: { typ: 0, min: 0, max: 0 }
  vil: { typ: 0.35, min: 0.3, max: 0.4 }
  vih: { typ: 0.75, min: 0.7, max: 0.8 }
  c_comp: { typ: 1.2e-12, min: 1.0e-12, max: 1.4e-12 }
  r_load: 50
  sim_time: 10e-9